# Copy this file to .env and fill in your real values.
# .env is gitignored — never commit real keys.

PORT=4000

# Supabase — see server/supabase/README.md for setup steps
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET=road-images
